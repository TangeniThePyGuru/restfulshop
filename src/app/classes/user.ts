export class User {
    constructor(
        id: number,
        public name: string,
        public email: string,
        public admin: boolean,
        // avatar: string,
        // joined: object
    ) {

    }
}
